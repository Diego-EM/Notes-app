import { NoteContainerDirective } from './note-container.directive';

describe('NoteContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteContainerDirective();
    expect(directive).toBeTruthy();
  });
});
